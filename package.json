{
    "name": "civix-ai-workspace",
    "private": true,
    "scripts": {
        "build": "cd client && npm install && npm run build",
        "start": "cd server && node server.js"
    },
    "devDependencies": {
        "concurrently": "^8.2.1"
    }
}